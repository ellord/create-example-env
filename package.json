{
  "name": "create-example-env",
  "version": "1.0.0",
  "description": "Create an example env file based on process.env usage from your source code",
  "main": "dist/index.js",
  "bin": {
    "create-example-env": "dist/index.js"
  },
  "engines": {
    "node": ">=12"
  },
  "scripts": {
    "build": "npm run build:types && npm run build:js",
    "build:js": "babel src --out-dir dist --extensions \".ts\" --source-maps inline && chmod u+x dist/index.js",
    "build:types": "tsc --emitDeclarationOnly",
    "start": "babel-node -x \".ts\" src/index.ts",
    "test": "jest",
    "type-check": "tsc --noEmit",
    "type-check:watch": "npm run type-check -- --watch",
    "watch": "tsc -w"
  },
  "author": "Ellord Tayag",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.15.7",
    "@babel/core": "^7.15.5",
    "@babel/node": "^7.15.4",
    "@babel/plugin-proposal-class-properties": "^7.14.5",
    "@babel/preset-env": "^7.15.6",
    "@babel/preset-typescript": "^7.15.0",
    "@types/jest": "^27.0.2",
    "@types/node": "^12.20.26",
    "jest": "^27.2.1",
    "prettier": "^2.4.1",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "ts-morph": "^12.0.0"
  }
}
